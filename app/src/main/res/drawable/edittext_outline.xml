<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" />

    <!-- Outline/Stroke properties -->
    <stroke
        android:width="2dp"
        android:color="#FF9900" /> <!-- Orange outline, you can change the color here -->

    <!-- Rounded corners -->
    <corners android:radius="12dp" /> <!-- Adjust the radius to your preference -->

    <!-- Padding inside the EditText -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp" />
</shape>